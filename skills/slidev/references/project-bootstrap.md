# Project Bootstrap

Setup checklist for new Slidev projects.

## Checklist

### Node.js Version
- Create `.nvmrc` with `24`

### VS Code
- Create `.vscode/settings.json`:
  ```json
  {
    "editor.formatOnSave": false
  }
  ```

---
_Add new bootstrap steps above this line._